#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    int A[10], n=10,i;
    int sum=0;
    cout<<"Enter the no.";
    for(i=0;i<n;i++)
    {
        cin>>A[i];
    }
    for(i=0;i<A[i];i++)
    { 
        sum+=A[i];
    }
    cout<<sum;
    return 0;
}