#include<iostream>
using namespace std;
int main()
{
    int a;
    cout<<"Enter the no.";
    cin>>a;
    if(a>0)
    cout<<"It's positive no.";
    else if(a<0)
    cout<<"It's negative no.";
    else if (a==0)
    cout<<"zero";
    return 0;

}